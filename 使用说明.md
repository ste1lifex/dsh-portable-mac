# DSH (DeepSeek Harness) — macOS 桌面版使用说明

**Apple Silicon（M1/M2/M3/M4）专用**。一个原生窗口，顶部是控制条，下面是内嵌的 DSH 界面；
形态与 Windows 上的 `DshDesktop.exe` 一致。

---

## 一、安装（3 步）

1. **完整解压** `DSH-mac-arm64.zip`（双击即可，会解出 `DSH.app` 和 `安装.command`）。
2. **双击 `安装.command`**，按提示回车。脚本会：
   解除下载隔离标记 → 修好可执行权限 → **ad-hoc 临时签名（1–3 分钟）** → 询问是否把
   `DSH.app` 复制到「应用程序」并启动。
   > 双击没反应或提示权限不足时，打开「终端」：
   > ```bash
   > cd /路径/到/解压出的目录
   > bash 安装.command
   > ```
3. **也可以直接把 `DSH.app` 拖进「应用程序」**：
   - 从浏览器下载的包带隔离标记，第一次打开若被拦下，右键 App →「打开」→「打开」；
   - 若提示「已损坏，无法打开」，说明隔离标记还在并且签名未修复，这时仍需跑一次
     `安装.command`，或手动执行：
     ```bash
     xattr -dr com.apple.quarantine /Applications/DSH.app
     codesign --force --deep --sign - /Applications/DSH.app
     ```

首次启动会**自动准备数据目录**（顶部控制条会显示「首次运行：正在展开…」）。

- 随包**带**离线缓存时：约 1–2 分钟，全程不联网；
- 随包**不带**离线缓存时（GitHub 上的默认发行版）：需要联网安装依赖，约 1–3 分钟。

之后每次启动都是秒开。

---

## 二、关于「无法验证开发者」警告

本 App **没有 Apple 开发者签名**（那需要 99 美元/年的账号 + 一台 Mac）。
`安装.command` 用的是 **ad-hoc 临时签名**，足以在本机正常使用，但：

- 如果之后你用浏览器重新下载并解压，隔离标记会回来，需要**重新跑一次 `安装.command`**；
- 从 U 盘 / 局域网拷贝过去的通常没有隔离标记，脚本会提示「跳过」；
- 想彻底消除警告，只能购买 Apple 开发者账号做正式签名 + 公证。

> ⚠️ 如果你自己改过 App 包内的文件（壳代码、图标等），签名会失配，同样需要重新签名：
> `codesign --force --deep --sign - DSH.app`。

---

## 三、控制条（顶部一行）

| 区域 / 按钮 | 作用 |
| --- | --- |
| 最左「运行中 / 未启动」 | 服务状态（绿点 = 运行中） |
| 启动 / 停止 / 重启 | 管理内嵌的 DSH 服务进程 |
| 刷新界面 | 重新加载 Web UI |
| 浏览器打开 | 用系统默认浏览器打开**带认证 token 的地址** |
| 日志 | 打开日志目录（`~/Library/Application Support/DSH/logs`） |
| 版本信息 | 桌面外壳 / 核心版本、插件版本、端口、数据目录、**错误日志末尾（可选中复制，另有「复制全部」）** |
| 最右「余额」 | DeepSeek 官方账户余额：悬停看总计/赠送/充值明细与更新时间；点击立刻刷新；每 5 分钟自动刷新（失败降到 1 分钟重试）。余额 < 10 或账户不可用时数字变琥珀色 |

**外观**：控制条与启动页**跟着内嵌界面一起浅色/深色**（网页端跟随系统或你在
设置里选的 `ui-theme.preference`）；Dock 图标也会随 macOS 外观在深/浅两版之间切换。
窗口变窄时按钮自动收成图标（悬停有提示）。

---

## 四、关于浏览器地址（0.1.5 起的新行为）

新版核心启用了 **Web 认证（进程级 token）**：

- App 自己会把**带本次进程认证 token 的地址**装进内嵌窗口，直接用即可；
- **手动在浏览器输入 `http://127.0.0.1:3099` 这类裸地址会收到 401**，这是正常的；
- 想用系统浏览器访问（点控制条「浏览器打开」即可），或从日志里取带 token 的地址：
  ```bash
  grep -a '^dsh web: ' ~/Library/Application\ Support/DSH/logs/dsh-web.out.log | tail -1
  ```
  输出形如 `dsh web: http://127.0.0.1:3099/?token=...`，整条复制到浏览器打开。

---

## 五、数据放在哪

| 内容 | 位置 |
| --- | --- |
| 会话 / 设置 / 凭证 / 插件配置 | `~/Library/Application Support/DSH/dsh-home/` |
| 依赖与离线缓存 | `~/Library/Application Support/DSH/{app-npm,store}/` |
| 日志 | `~/Library/Application Support/DSH/logs/` |
| 只读运行时（Node、初始数据） | App 包内 `DSH.app/Contents/Resources/runtime/` |

升级 App 不会动数据目录。备份只需备份 `~/Library/Application Support/DSH/dsh-home/`。
想彻底重来：退出 App，删掉 `~/Library/Application Support/DSH`，重新启动 App。

---

## 六、已知差异与限制

1. **dsh-doc 文档/OCR 插件在 macOS 上默认不启用**：它的运行时只有 Windows 版
   （`dshdoc-runtime-win32-x64`），装上只会让插件树加载失败；本发行版的 profile 里
   已不包含它，**不影响对话与其它插件**。
2. **Windows 电脑控制插件（dsh-computer-use-win）仅 Windows 可用**，macOS 发行版不含。
3. **pwsh 工具需要另装 PowerShell Core**（agent 要执行命令时才需要）：
   ```bash
   brew install --cask powershell
   ```
   不装也能正常用界面与对话（bash 工具可用）。
4. **仅支持 Apple Silicon（arm64）**；Intel Mac 需要另做 x64 版本。
5. **不带离线缓存时首启需要联网**（从 npm registry 安装核心与插件依赖）；
   想要完全离线，请在构建时把 `store.tar.gz` 放进包内（见仓库 README「用法二」）。

---

## 七、排错

| 现象 | 处理 |
| --- | --- |
| 双击 `安装.command` 没反应 | 用终端执行 `bash 安装.command` |
| 「已损坏，无法打开」 | 隔离标记未清或签名失效：重跑 `安装.command`，或手动 `xattr -dr com.apple.quarantine DSH.app && codesign --force --deep --sign - DSH.app` |
| 「无法验证开发者」 | 右键 App →「打开」→「打开」；或重跑 `安装.command` |
| 启动后一直停在「正在准备…」 | 看 `~/Library/Application Support/DSH/logs/dsh-web.out.log`；控制条「版本信息」里也有错误日志 |
| 窗口空白 / 401 | 点控制条「重启」，或从日志取带 token 的地址用浏览器打开 |
| 余额显示「—」 | 悬停看原因：没配 `DEEPSEEK_API_KEY`（在 `~/Library/Application Support/DSH/app-npm/.env`）/ 密钥失效 / 网络不通 |
| 想彻底重来 | 退出 App，删除 `~/Library/Application Support/DSH`，重新启动 App |

---

## 八、当前版本

- 桌面外壳 **0.1.5**
- 核心 `@deepseek-ai/dsh` **0.1.5-rc.2**
- Web UI 全家桶 `@linxin666/dsh-web-all` **^0.3.23**、`dsh-free-search` **0.4.32**
- 本地插件 `dsh-endfield-boot`、`dsh-pet-perlica`（宠物默认关闭，可在设置里开启）
- 运行时：Electron **44.3.0**、Node **v24.19.0**、pnpm **11.19.0**
